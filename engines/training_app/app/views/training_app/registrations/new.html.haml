#main.container.row.training
  #new_registration
    .row
      .columns.four.centered
        = image_tag 'training_app/loader.gif', style: 'display: block; margin: 1em auto; text-align: center;'
        %h3 Loading payment form...

- content_for(:javascript) do
  :javascript
    $(function(){
      TrainingApp.initialize(#{@registration.to_json(include: :course)})
    })
